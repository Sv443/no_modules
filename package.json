{
  "name": "no_modules",
  "version": "1.0.0",
  "description": "Deletes all node_modules folders recursively, from a provided starting path",
  "main": "index.ts",
  "scripts": {
    "start": "node --max-old-space-size=4096 -- node_modules/.bin/ts-node index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sv443",
  "license": "MIT",
  "dependencies": {
    "fs-extra": "^11.1.1",
    "prompts": "^2.4.2"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.1",
    "@types/prompts": "^2.4.4",
    "ts-node": "^10.9.1"
  }
}